# =============================================================================
# Render Check — validates that the Quarto site renders without errors
# Runs on every pull request to the main branch.
# =============================================================================
name: Render Check


env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  pull_request:
    branches: [main]

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2

      - name: Install R dependencies
        run: |
          Rscript -e 'install.packages(c("knitr", "rmarkdown", "yaml"), repos = "https://cloud.r-project.org")'

      - name: Setup Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Render site
        run: quarto render

      - name: Check output
        run: |
          if [ -d "docs" ]; then
            echo "✅ Site rendered successfully."
            echo "Files generated:"
            find docs -type f | head -20
          else
            echo "❌ Site output directory not found."
            exit 1
          fi
