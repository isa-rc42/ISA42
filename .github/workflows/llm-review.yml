# =============================================================================
# LLM Review — PLACEHOLDER workflow
# Future integration: when a GitHub Issue is labeled "needs-review",
# this workflow will call an LLM API to prepare a draft for human review.
#
# IMPORTANT: This workflow is NOT active. It requires:
# - An OPENAI_API_KEY (or equivalent) stored as a GitHub Secret
# - A Python script (scripts/llm_review_submission.py) fully implemented
# - Human approval before any LLM-generated content is published
# =============================================================================
name: LLM Review (Placeholder)

# This workflow is currently disabled. Uncomment the 'on' block to activate.
# on:
#   issues:
#     types: [labeled]

on:
  workflow_dispatch:  # Manual trigger only for now

jobs:
  llm-review:
    # Only run when the "needs-review" label is added
    # if: github.event.label.name == 'needs-review'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pyyaml
          # TODO: Add openai or other LLM client library
          # pip install openai

      - name: Run LLM review script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # TODO: Add your LLM API key as a GitHub Secret
          # OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          echo "🔮 LLM Review workflow — PLACEHOLDER"
          echo "This workflow will:"
          echo "  1. Read the issue body"
          echo "  2. Send it to an LLM for draft preparation"
          echo "  3. Create a PR with the draft content"
          echo "  4. Require human approval before merge"
          echo ""
          echo "To activate, configure OPENAI_API_KEY and implement scripts/llm_review_submission.py"
          # python scripts/llm_review_submission.py --issue-number ${{ github.event.issue.number }}
